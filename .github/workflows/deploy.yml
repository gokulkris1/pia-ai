name: Deploy disabled for M1

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Manual GCP/Firebase deploy only
        run: echo "Automatic legacy deploys are disabled. Production deploys to Cloud Run + Firebase Hosting."
