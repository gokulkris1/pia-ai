name: Deploy disabled for M1

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Manual GCP deploy only
        run: echo "Automatic Netlify deploy is disabled. Milestone 1 deploys manually to Cloud Run + Firebase Hosting."
